# solidity-example

## Usage

Require [nodejs](https://nodejs.org/en/).

First, `npm i`

##

auto compile `.sol` files to `./modified-files/`

```
$ npm run dev
```

compile `.sol`

```
$ npm run compile
```
